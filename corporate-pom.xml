<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.placeholder</groupId>
	<artifactId>corporate-parent</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>pom</packaging>

	<!-- corporate pom is a maven best practice see:
  https://books.sonatype.com/mvnref-book/reference/pom-relationships-sect-pom-best-practice.html
  https://blog.sonatype.com/2008/05/misused-maven-terms-defined/#coporate-pom
  https://stackoverflow.com/questions/33522364/nexus-maven-corporate-pom-what-should-it-contain -->
	<name>placeholder Corporate Parent POM</name>
	<organization>
		<name>placeholder</name>
	</organization>

	<distributionManagement>
		<repository>
			<id>releases</id>
			<name>placeholder-releases</name>
			<url>https://maven.placeholder.com:443/artifactory/libs-release-local</url>
		</repository>
		<snapshotRepository>
			<id>snapshots</id>
			<name>placeholder-snapshots</name>
			<url>https://maven.placeholder.com:443/artifactory/libs-snapshot-local</url>
		</snapshotRepository>
	</distributionManagement>

	<properties>
		<!-- locking down plugin versions, maven best practice -->
		<!-- see https://blog.sonatype.com/2008/04/maven-209-released/ -->
		<assembly.version>2.6</assembly.version>
		<clean.version>2.6.1</clean.version>
		<compiler.version>3.8.0</compiler.version>
		<deploy.version>2.8.2</deploy.version>
		<enforcer.version>3.0.0-M1</enforcer.version>
		<formatter.version>2.9.0</formatter.version>
		<install.version>2.5.2</install.version>
		<!-- default JDK version properties -->
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
		<maven.version>3.5.0</maven.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<!-- sonar maven plugin version for sonarqube analysis -->
		<sonar.version>3.6.0.1398</sonar.version>
		<sortpom.version>2.10.0</sortpom.version>
		<surefire.version>3.0.0-M3</surefire.version>
	</properties>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-clean-plugin</artifactId>
					<version>${clean.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>${compiler.version}</version>
					<configuration>
						<nonFilteredFileExtensions>
							<nonFilteredFileExtension>p12</nonFilteredFileExtension>
							<nonFilteredFileExtension>crt</nonFilteredFileExtension>
							<nonFilteredFileExtension>jks</nonFilteredFileExtension>
						</nonFilteredFileExtensions>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-assembly-plugin</artifactId>
					<version>${assembly.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>${surefire.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-install-plugin</artifactId>
					<version>${install.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-deploy-plugin</artifactId>
					<version>${deploy.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-enforcer-plugin</artifactId>
					<version>${enforcer.version}</version>
				</plugin>
				<plugin>
					<groupId>org.sonarsource.scanner.maven</groupId>
					<artifactId>sonar-maven-plugin</artifactId>
					<version>${sonar.version}</version>
				</plugin>
				<plugin>
					<groupId>net.revelc.code.formatter</groupId>
					<artifactId>formatter-maven-plugin</artifactId>
					<version>${formatter.version}</version>
				</plugin>
				<plugin>
					<groupId>com.github.ekryd.sortpom</groupId>
					<artifactId>sortpom-maven-plugin</artifactId>
					<version>${sortpom.version}</version>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<artifactId>maven-enforcer-plugin</artifactId>
				<executions>
					<execution>
						<id>enforce-maven</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireMavenVersion>
									<version>${maven.version}</version>
									<message>This project requires at least Maven ${maven.version}</message>
								</requireMavenVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>net.revelc.code.formatter</groupId>
				<artifactId>formatter-maven-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>format</goal>
						</goals>
						<phase>verify</phase>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>com.github.ekryd.sortpom</groupId>
				<artifactId>sortpom-maven-plugin</artifactId>
				<configuration>
					<createBackupFile>false</createBackupFile>
					<expandEmptyElements>false</expandEmptyElements>
					<nrOfIndentSpace>-1</nrOfIndentSpace>
					<lineSeparator>\n</lineSeparator>
					<keepBlankLines>true</keepBlankLines>
					<predefinedSortOrder>recommended_2008_06</predefinedSortOrder>
					<sortProperties>true</sortProperties>
					<verifyFail>Stop</verifyFail>
				</configuration>
				<executions>
					<execution>
						<id>sort-pom</id>
						<goals>
							<goal>sort</goal>
						</goals>
						<phase>verify</phase>
					</execution
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>jenkins-ci</id>
			<activation>
				<property>
					<!-- activates when ran from jenkins agent -->
					<name>env.HOME</name>
					<value>/home/jenkins</value>
				</property>
			</activation>
			<build>
				<plugins>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
